/*
import React from "react";
import styles from "../styles/ChatListItem.module.scss";

const ChatListItem = ({ chat, onClick, isSelected }) => {
    return (
        // <div className={`${styles.chatListItem} ${isSelected ? styles.selected : ''}`} onClick={onClick}>
        //     <img src={chat.profileIcon} alt="profileIcon" className={styles.profileIcon}/>
        //     <div className={styles.chatDetails}>
        //         <div className={styles.userName}>{chat.userName}</div>
        //         <div className={styles.lastMessage}>{chat.lastMessage}</div>
        //     </div>
        // </div>
    );
};

export default ChatListItem;*/
